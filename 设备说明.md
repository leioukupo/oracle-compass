# 设备说明：MAGNEO C110001（MT6580）

## 基本信息

| 项目 | 值 |
| --- | --- |
| 品牌 / 型号 | MAGNEO / C110001 |
| 平台 | MediaTek MT6580 |
| 系统 | Android 5.1（API 22，LMY47I），Build MAGNEO_L01_20161231（user/test-keys） |
| 内核 | Linux 3.10.72+ SMP PREEMPT（2016-12-31） |
| Root | 已 root（Magisk，ro.secure=0 / ro.debuggable=1） |
| 序列号 | 11B010375A |
| adb 连接 | 192.168.31.7:5555（已连接） |

## 处理器 / 内存 / 存储

| 项目 | 值 |
| --- | --- |
| CPU | 四核 Cortex-A7（ARMv7）@ 最高 1.3GHz（当前仅 1 核在线） |
| 内存 | 2GB（2028MB） |
| 存储 | eMMC 16GB 级：/data 11.9G（可用 11.5G）、/system 1.5G、/sdcard 11.9G |

## 显示

800×800 圆形屏幕，density 320（xhdpi），物理显示尺寸为直径 92mm 的圆。

## GPS

- MTK 集成 GPS（HAL：gps.default.so，ro.mtk_gps_support=1）
- 支持 A-GPS：SUPL 1.0（默认 supl.google.com:7275），agpsd、wifi2agps 服务运行中
- 支持 MTK EPO 星历预载（mtk_gps_inject_epo_data）

## WiFi

- MTK 集成 WLAN（mediatek.wlan.chip=CONSYS_MT6735，固件 WIFI_RAM_CODE_6580），2.4GHz 802.11 b/g/n 单频
- 支持 Wi-Fi Direct（p2p0）、软 AP（ap0）、WSC、MCC；默认 SSID：MAGNEO
- 当前状态：wlan0 已连接 192.168.31.12（网关 192.168.31.1，DHCP）

## 蓝牙

- MTK 集成蓝牙（Blueangel 协议栈，init.svc.mtkbt），蓝牙 4.0 + BLE（RPA/IRK/广播/扫描过滤配置齐全）
- 支持 A2DP / HFP / AVRCP / OPP / FTP / DUN / SIMAP 等 Profile（AVRCP 输入设备已注册）
- 支持无损蓝牙音频（ro.mtk_lossless_bt_audio=1）

## 摄像头（MediaTek Camera Module，共 2 个）

### 后摄（Camera 0）

- 朝向：BACK，方向 90°
- 支持自动对焦（auto/macro/infinity/连续对焦等）与闪光灯（已声明 autofocus/flash 特性）
- 照片最大 2736×2736（方形档位：560/800/1040/1600/2048/2592/2736）
- 预览最大 3264×2448（约 8MP 传感器），另有 1920×1080 等档位
- 视频档位含至 3840×2160（MT6580 实际录像能力通常上限 1080p）

### 前摄（Camera 1）

- 朝向：FRONT，方向 270°
- 照片最大 1040×1040（方形档位：560/800/1040）
- 预览支持 176×144 至 1920×1080 档位

> 厂商库（libcameracustom.so / libcam.halsensor.so）内置传感器型号表：GC2355、GC0310、IMX145、IMX179、IMX219、OV5670、OV5671、OV5693、OV8858、OV9724、OV9760；实际装机型号以后摄约 8MP、前摄约 1MP 档位推断。

## 传感器

MTK 硬件传感器 6 个：加速度计、陀螺仪、磁力计（地磁）、光线、距离（接近）、方向；另有系统融合的旋转矢量/重力/线性加速度传感器。

## 其他外设

- 触摸屏：Himax 电容触摸（himax-touchscreen + mtk-tpd）
- 音频：mt-snd-card（MTK），支持 APE 无损
- FM 收音：支持（fmradio.driver.enable=1）
- 电池：Li-ion，当前约 4138mV / 25°C；充电 IC 为 TI bq24296；电源节点 ac/battery/usb/wireless（含无线充电节点）
- USB：MediaTek VID 0E8D，MTP + ADB
- 其他：震动马达、LED（lights）、按键（mtk-kpd）、耳机检测（ACCDET）

*以上信息采集自设备实时状态（getprop / dumpsys / sysfs / 系统库文件），采集时间 2026-07-31。*

## GPS / AGPS（2026-08-06 调整）

### 硬件与现状

- GPS 位于 MT6620 组合芯片内（`ro.mtk_gps_support=1`），守护进程：mnld（GPS 引擎）、mtk_agpsd（AGPS）、wifi2agps（WiFi 辅助）
- 实测（室外 10 分钟）：`$GPGSV,1,1,0` 可见卫星始终为 0，`GET_RTC_FAIL` 每轮上报——**数字端正常、射频端收不到信号，判断为天线/硬件层面问题**（排除法：AGPS 配置、provider 重启、系统重启均无效）
- 双 SIM 槽当前均 NOT_READY（未插卡）；无 SIM 时走 WiFi，国内没有任何可达的 SUPL 服务器

### 已修改的系统文件（root，已备份）

1. `/system/etc/agps_profiles_conf2.xml`（备份 `.bak`）
   - `cur_supl_profile` 从 GOOGLE(supl.google.com:7275) 改为 **CMCC(221.176.0.55:7275)**
   - CMCC profile 增加 `mcc_mnc="46000"` 自动选择
   - 新增 CUCC（`h-slp.mnc001.mcc460.pub.3gppnetwork.org`，mcc_mnc=46001）与 CTCC（`h-slp.mnc011.mcc460.pub.3gppnetwork.org`，mcc_mnc=46011）
2. `/system/etc/gps.conf`（原文件不存在，全新创建）
   ```
   NTP_SERVER=ntp.aliyun.com
   SUPL_HOST=221.176.0.55
   SUPL_PORT=7275
   ```

### 生效条件与说明

- 上述改动**需重启生效**（框架 GPS HAL 启动时读取 gps.conf）
- 运营商 SUPL 服务器（221.176.0.55 等）只在**运营商移动数据网络内可达**：插入对应 SIM 后 AGPS 才会真正工作（自动按 MCC/MNC 选择 CMCC/CUCC/CTCC）
- 无 SIM、纯 WiFi 时：SUPL 全部不可达（Google 被墙、运营商内网不进公网），此时星历依赖 **MTK ePO**（`/data/misc/EPO.DAT`，走 HTTP 国内可达，2026-08-06 实测有下载）
- 重置系统后需重新执行上述两处文件修改（gps.conf 为新文件，agps_profiles_conf2.xml 恢复默认 GOOGLE）

### WiFi/IP 兜底定位（App 内，2026-08-06 新增）

GPS 射频不可用时，App 自动启用网络定位兜底：

- 主界面/网页状态显示 `定位(IP) 纬度,经度 ±精度m`；GPS 有 fix 时优先显示 GPS
- 流程：WiFi 扫描 BSSID → Mozilla Location Services（WiFi 级百米精度，需有效 key）→ 失败自动降级 ip-api.com（IP 级约 5km，国内可达免 key）
- 实现：[WifiLocator.java](app/src/main/java/com/magneo/compass/WifiLocator.java)，每 30s 更新一次，随主界面启停
- 提升精度：去 location.services.mozilla.com 注册免费 key 替换 `?key=test`，或申请高德/腾讯定位 key 后把请求 URL 换到对应服务（代码里集中一处）

### 视频播放（2026-08-06 更新）

MT6580 硬解上限为 H.264 ≤1080p，NAS 上的 4K/HEVC/10bit 视频无法直解（表现为黑屏有声）。**NAS 转码方案已移除**：fnOS 未提供可供第三方调用的转码接口（飞牛影视的内部 API 只面向影视库内已入库媒体，不能给网盘里任意文件转码），且该方案要多占一个端口、依赖外部脚本，不可靠。

- 播放器检测到无法解码（8 秒无画面渲染或播放出错）时，直接显示「视频无法解码」提示并停止播放，不再黑屏干等、不再尝试转码
- 文件列表长按视频不再有「NAS 转码播放」入口；网页设置不再显示 NAS 转码配置
- 需要看 NAS 上的高规格视频：建议先用飞牛影视入库后走它自带的服务端转码，或在电脑端观看
